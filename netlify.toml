[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build]
  command = "npm run build"
  publish = "build"
  functions = "functions-build"
  node_bundler = "esbuild"

[functions]
  directory = "functions-build"
  node_bundler = "esbuild"
